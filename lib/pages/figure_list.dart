import 'package:flutter/material.dart';

class FigureListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Figures created by you'),);
  }
}
